To begin the project, I started off by creating a new repository in GitHub. I used the git clone command in Visual Studio Code terminal to clone the repository to my local machine. and created an index.html file. I created an index.html file and created a new branch for feature/footer. In the HTML file, I added a basic HTML structure along with a header. I staged the file with the git add command, then committed the file. I switch back into the main branch with the git checkout command. Next, I created a branch for feature/footer. I put the footer in the HTML and merged the branch into main. I encountered a merge conflict when merging the feature/header branch into main. I edited the HTML so that both sections were included. I used git status to see if any conflicts remained. I then committed the resolved file and pushed my final changes to GitHub. Creating a pull request helps ensure code quality by providing a structured way to review additions and changes before merging them into other branches. A pull request allows you to compare branches to see what changes were made.
